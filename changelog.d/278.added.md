Generate a typed, scheme-scoped OpenAPI security plan and executable package entrypoint. CLI, MCP, and static schema commands now project one registry; preserve authentication alternatives and conjunctions; validate base URLs and credentials before publication; and use portable collision-disambiguated environment names without carrying secret values.
