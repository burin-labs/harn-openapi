Add a typed OpenAPI application-error model with deterministic exact-status,
status-range, and default matching plus a closed fallback that cannot expose an
undeclared upstream body. Raw adapters now consistently treat every non-2xx
HTTP response as an error.
